# Every `tk` subcommand the image's run scripts invoke, one per line.
#
# DERIVED, not maintained by hand: it is exactly what
# factory.EntrypointTkCommands() reads out of entrypoint.sh, worker.sh,
# common.sh and preflight.sh, and TestRequiredTkCommandsFileMatchesTheEntrypoint
# fails the build when the two drift. It exists as a file because the image
# build reads it too — the last layer of the Dockerfile runs every line against
# the tk it just built, so an image whose tk predates one of these fails to
# build instead of booting a container that dies mid-run with "unknown command".
answer
cloud branch
list
sandbox environment
sandbox image
sandbox model
sandbox setup
sandbox substrate
sandbox toolchain
sandbox worker-prompt
version
