/tk
/tickboard

# tk worktrees
.worktrees/

# Test auth credentials (generated by setup-cloud-auth.sh)
.test-auth.env

# wrangler-ctl state files
internal/tickboard/ui/e2e/.wrangler.*

# tk Claude worktrees
.claude/worktrees/

# Vitest/Vite scratch state, written at the repo root when a workspace suite is
# run from here rather than from its own package directory.
node_modules/

# repo-wiki local ingest state
repo-wiki/.ingest/
repo-wiki/.comments/
bin/

# Python bytecode from scripts/ (the benchmark harness is a module when imported)
__pycache__/
*.pyc
