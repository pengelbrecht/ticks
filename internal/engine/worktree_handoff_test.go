package engine

// Worktree lifecycle tests have been removed.
// The engine no longer manages worktree creation/cleanup.
// Worktree management is handled by run.go (the caller).
// See the parallel runner and run.go for worktree integration tests.
