{
  "id": "t9k",
  "title": "Fix PBKDF2 iterations: 210k exceeds Cloudflare's platform cap",
  "description": "BLOCKER found by the first live deploy (https://ticks-factory.pe-1a0.workers.dev). cloud/factory/src/auth.ts ships DEFAULT_ITERATIONS = 210_000 and MAX_ITERATIONS = 1_000_000. Cloudflare Workers caps PBKDF2 at 100_000 iterations: crypto.subtle.deriveBits THROWS on the edge above that, so every authenticated request returns 503 auth_not_configured on a correctly deployed factory.\n\nMeasured live, same token, same code, only the iteration count changed:\n- 210_000 -\u003e 503 (deriveBits throws)\n- 100_000 -\u003e 404 on /api/runs (authenticated; route simply does not exist yet). Also verified: no token -\u003e 401, wrong token -\u003e 401.\n\nThree fixes, all needed:\n1. DEFAULT_ITERATIONS and MAX_ITERATIONS must both be \u003c= 100_000. Note aro's own concern #2: the token is 256 bits of CSPRNG output, so stretching buys nothing against brute force the way it does for a password — going lower is defensible on the merits, not just the cap.\n2. authenticateFactoryRequest's catch reports EVERY throw as 'not a valid \u003cscheme\u003e record'. That is wrong and it actively misdirected diagnosis: /health said configured:true (parse succeeded) while the request path said the record was invalid (crypto threw). Distinguish a parse failure from a crypto/runtime failure and say which.\n3. Local workerd does NOT enforce the cap — 60/60 tests passed on a value that cannot work in production. Add a guard test asserting DEFAULT_ITERATIONS \u003c= 100_000 and MAX_ITERATIONS \u003c= 100_000, with a comment citing the platform limit, so no future change can reintroduce this without a red test.\n\nThe live deployment currently runs a hand-pushed 100k record so it works; the code does not.",
  "notes": "2026-08-19 16:49 - runner-state: substrate=herdr kind=claude branch=tick/t9k worktree=/Users/peterengelbrecht/.herdr/worktrees/ticks/tick-t9k workspace=wA8 base=643def4de055bf58f4190dd7bbfc181e47dc52e1 agent=tick-t9k pane=wA8:p1 session=d4ca8f9b-c689-4c07-b53b-66659b6b126d\n2026-08-19 17:09 - Integrated gate FAILED after merge: the fix covered only the TypeScript side. internal/factory/token.go still has DefaultIterations = 210_000 and maxIterations = 1_000_000, so tk factory deploy would keep minting records the Cloudflare edge cannot verify — the exact production break this tick exists to fix. Caught by TestGoldenVectorMatchesWorker (7c1's cross-implementation test), which now reports auth.ts deriving at 100000 while this package pins 210000. Reopening for the Go half.",
  "status": "closed",
  "priority": 2,
  "type": "bug",
  "owner": "noreply@anthropic.com",
  "parent": "63t",
  "discovered_from": "aro",
  "acceptance_criteria": "DEFAULT_ITERATIONS and MAX_ITERATIONS are both \u003c= 100_000; a test fails if either is raised above the cap; the auth middleware distinguishes parse failure from crypto failure in its 503 detail and logs; existing auth tests still pass.",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-08-19T14:48:18.024186Z",
  "updated_at": "2026-08-19T15:34:30.066459Z",
  "closed_at": "2026-08-19T15:34:30.066459Z",
  "closed_reason": "Completed: PBKDF2 capped at Cloudflare's 100k platform limit on BOTH sides — cloud/factory/src/auth.ts and internal/factory/token.go — with guard tests in each language that go red if the cap is raised, and the auth middleware now distinguishes a parse failure from a crypto failure instead of misreporting both as an invalid record."
}