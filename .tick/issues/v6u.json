{
  "id": "v6u",
  "title": "Explicit orchestrator-location signal (TICKS_ORCHESTRATOR), not inference",
  "description": "Operator observation (2026-08-20), prompted by the first completed cloud run aborting on the repo-pinned herdr substrate.\n\nToday nothing tells an orchestrator WHERE it is running. The skill detects the HARNESS by capability probes (Agent tools, rlm in globals, /ticks-run registered) and resolves the SUBSTRATE by probing for a herdr socket. Neither answers \"am I a laptop or a container\", so a cloud orchestrator reads a repo config written for a laptop and does the wrong thing — exactly what eb5 is fixing case-by-case.\n\nAdd an explicit signal the entrypoint sets and both tk and the skill honour: TICKS_ORCHESTRATOR=cloud (values: local | cloud). It generalises beyond substrate:\n- substrate resolution: cloud implies harness in Phase 1, without probing for a socket that cannot exist.\n- max_parallel and other [orchestration] keys tuned for a laptop should not silently govern a container.\n- TK_ACTOR already carries cloud:orchestrator for PROVENANCE; this is the same fact available for ROUTING, so the two must agree rather than being derived separately.\n\nDesign constraints: absence means local, so nothing changes for existing users; the value is set by the sandbox entrypoint, never inferred from the environment; and a cloud orchestrator must still ANNOUNCE what it resolved and why, since the protocol requires explicit degradation to be stated rather than silent. Document it in the skill (agent-runner.md substrate section) alongside the existing probes, and note it in runners-config.md.\n\nCoordinate with eb5, which is fixing the substrate case right now — this tick is the general mechanism, and eb5 should use it rather than a one-off.",
  "notes": "2026-08-20 14:25 - Coordinate with z1b in the same wave: z1b owns internal/factory (deploy rollout wait); you own the entrypoint/skill signal. Both touch cloud/sandbox/entrypoint.sh — keep edits minimal and additive there. eb5 has already landed a substrate resolution in internal/sandbox/substrate.go; make that read the explicit signal rather than adding a second mechanism beside it.\n2026-08-20 14:26 - runner-state: substrate=herdr kind=claude branch=tick/v6u worktree=/Users/peterengelbrecht/.herdr/worktrees/ticks/tick-v6u workspace=wC4 base=be27ad6e5b61188bea756fe3cf192f9de0f4869a agent=tick-v6u pane=wC4:p1 session=396999ea-c4b3-46b8-a8ac-d47a5e51e810\n2026-08-20 14:38 - [human] SCOPE CORRECTION (operator, 2026-08-20) — read before going further; this narrows the tick.\n\nThe operator's framing is better than the one this tick was written with: the question is not 'where is the orchestrator' but 'what is available here', and ticks ALREADY has that mechanism — substrate = \"auto\" probes for herdr and falls back to harness silently when absent. The motivating scenario is a repo sometimes driven through herdr and sometimes through the cloud factory, which is exactly what auto is for.\n\nSo the real defect is narrower than this description implies: THIS REPO PINS substrate = \"herdr\", a hard assertion that herdr is always reachable. True on a laptop, false in a container. With auto, the first cloud run would have probed, found no socket, and degraded — no new mechanism required.\n\nEvaluate and choose, saying which and why in the report:\nA. auto is the answer and the pin was the bug. Set this repo to auto, and reduce this tick to documentation plus (maybe) a clear failure when an explicit herdr pin is unsatisfiable. Uses what exists; adds no vocabulary.\nB. an explicit signal is still warranted for what CANNOT be probed. auto answers 'is herdr reachable', not 'should I use it' — a cloud sandbox might one day have a reachable socket (herdr-in-the-cloud is a documented open door) and still want harness. And [orchestration].max_parallel has no probe at all: a laptop's 3 should not govern a container.\n\nMy reading is A for substrate, B only for the unprobeable keys — do not add an env var that duplicates what auto already decides. If you land B for anything, it must be a THIN override consulted by the existing resolution path (eb5's internal/sandbox/substrate.go), never a parallel mechanism.\n\nAlso check: does the existing explicit-degradation announcement fire on the auto path? The protocol requires a degraded substrate to be stated and noted; if auto is silent by design (the code comments suggest auto/unavailable is), a cloud run falling back to harness would do so invisibly, which is its own small problem worth naming.",
  "status": "in_progress",
  "priority": 2,
  "type": "task",
  "owner": "noreply@anthropic.com",
  "parent": "ko8",
  "acceptance_criteria": "A cloud orchestrator resolves its substrate and orchestration settings from an explicit signal rather than by probing for absent local infrastructure; absence of the variable behaves exactly as today; the resolved location and its consequences appear in the run output; the skill and runners-config document it.",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-08-20T10:57:22.093976Z",
  "updated_at": "2026-08-20T12:38:55.084515Z",
  "started_at": "2026-08-20T12:25:51.260466Z"
}