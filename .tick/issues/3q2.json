{
  "id": "3q2",
  "title": "Orchestrator sandbox image and headless harness bootstrap",
  "description": "The container one cloud run boots. Define the sandbox image and its entrypoint: git plus the language toolchains the target repo needs, a pinned omp (or claude) CLI, the ticks skill installed the normal way, and an entrypoint that clones the repo at the submitted SHA, verifies the tk version, runs the .tick/config.md Environment pre-flight, exports TK_ACTOR=cloud:orchestrator, and starts the headless harness on the skill loop. Model traffic must point at the AI Gateway base URL from the environment, never a vendor default. Harness output streams continuously to stdout so the Workflow can ship it to R2 during the run, never export-at-exit. Independent of the worker-side routes, so it can land in wave 1.",
  "notes": "2026-08-19 18:17 - runner-state: substrate=herdr kind=claude branch=tick/3q2 worktree=/Users/peterengelbrecht/.herdr/worktrees/ticks/tick-3q2 workspace=wAE base=3e15bff81cd888e82dfe521c507a46f4ddf6913b agent=tick-3q2 pane=wAE:p1 session=3dffeaa1-43f1-43a1-9dd0-73f0891ddee3\n2026-08-19 18:23 - Design guidance (human question, 2026-08-19): the same image runs many times per repo with fresh clones — do NOT let that imply per-task image builds.\n\nLayering:\n1. BASE IMAGE, pinned to the tk version (same cadence as the factory bundle, which tk factory deploy already version-pins): git, the harness CLIs, language runtimes. Built and pushed at deploy time, instantiated per run. Starting the Nth container costs no rebuild.\n2. REPO DEPENDENCY CACHE — persisted, never baked into the image. Baking node_modules means rebuilding the image on every lockfile change, which is the treadmill this question is trying to avoid. Prefer a named sandbox per project whose filesystem survives sleep/wake (pnpm store, ~/.cache/go-build, ~/.cargo), so a fresh clone lands in a warm container. Snapshot/restore is the alternative.\n\nHARD CONSTRAINT (axiom 1): cache warmth is convenience state. A cold sandbox with an empty cache must always produce a CORRECT run — warmth buys speed, never correctness. Never let a green build depend on a warm worker. Verify the actual Sandbox persistence/snapshot API rather than assuming it; this epic already paid once for assuming a platform behaviour (the PBKDF2 cap).\n\nRelated gap, flagged not assigned: .tick/config.md has an Environment section, but it is VERIFICATION only (test, don't ask). There is no place to declare how to WARM a repo (pnpm install --frozen-lockfile in cloud/factory, go mod download). A 'Setup' section — idempotent, cache-populating commands — would give this entrypoint something to run and would help local runs too. If you think it belongs in this tick, say so in your report rather than adding it silently.",
  "status": "in_progress",
  "priority": 2,
  "type": "task",
  "owner": "noreply@anthropic.com",
  "parent": "ko8",
  "acceptance_criteria": "The image builds reproducibly and its entrypoint, given a repo URL, SHA and gateway base URL, reaches the point of running the skill loop; the pre-flight failure path exits nonzero with the failing check named; harness output appears on stdout as it is produced, not at exit.",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-08-19T15:56:17.741795Z",
  "updated_at": "2026-08-19T16:23:56.495879Z",
  "started_at": "2026-08-19T16:16:44.488385Z"
}