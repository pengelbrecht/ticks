{
  "id": "kji",
  "title": "Wave width is read from config, logged, then ignored under the harness substrate",
  "description": "Measured on run_62c289d1. The boot log states \"wave width 3 from .tick/runners.toml [orchestration].max_parallel — under the harness substrate that is concurrent subagents in one sandbox\", and .tick/runners.toml does set max_parallel = 3. The orchestrator then created seven worktrees (09w 201 3nh 5jo 5qj gm5 iit) and reported \"All 7 implementers dispatched. Now blocking on their completion.\" So the limit is parsed and echoed but never enforced on the dispatch path.\n\nConsequences beyond the obvious: concurrent burn is more than double what the operator configured, which is most of why a 45-minute run exhausted its budget; and seven implementers with divergent contexts share one x-session-affinity key, which is a plausible cause of the erratic prefix-cache misses seen in the same run (per-call hit rate swinging between 92 percent and 0 percent).\n\nEnforcement has to be on the dispatch path, not in the prompt — a width a model can talk itself out of is not a width. Add a test that a wave of N ticks with max_parallel = k never has more than k implementers in flight.",
  "notes": "2026-08-21 14:37 - Seam: wave 1 with uzx and l4l. You own the DISPATCH path — wherever the wave is fanned out under the harness substrate. uzx owns detectTrip/pollDelay in run-workflow.ts; l4l owns new cmd/tk/cmd files and the gateway log reader. Do not change trip logic.\n2026-08-21 14:37 - runner-state: substrate=herdr kind=claude branch=tick/kji worktree=/Users/peterengelbrecht/.herdr/worktrees/ticks/tick-kji workspace=wD3 base=c904ba26f8b4235d978617d4c7f9a08f7432ad8a agent=tick-kji pane=wD3:p1 session=95c7fde0-bb0c-40de-8174-984af587506d\n2026-08-21 16:55 - runner-state: runner=claude branch=tick/kji merged=0314e445\n2026-08-21 16:55 - Root cause was better than the tick's framing: tk graph --json reported the WIDEST WAVE under stats.max_parallel — the same key name [orchestration].max_parallel uses for dispatch width — and both SKILL.md and agent-runner.md said 'launch up to max_parallel subagents per wave'. On the 7-tick wave the graph answered 7 while the config said 3, so the orchestrator did what its tooling told it. Enforcement now lives on the claim (tk update --status in_progress), the one dispatch signal that is substrate-neutral and durable, refused with a new exit code 8.",
  "status": "closed",
  "priority": 0,
  "type": "task",
  "owner": "noreply@anthropic.com",
  "parent": "1vn",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-08-21T10:33:56.093187Z",
  "updated_at": "2026-08-21T14:55:29.84261Z",
  "closed_at": "2026-08-21T14:55:29.84261Z",
  "closed_reason": "Completed: the configured wave width is now enforced where every substrate dispatches — the claim — rather than printed at boot and hoped for, and the tooling that told the orchestrator to launch seven has been corrected. A slot is live rather than a budget, so closing or releasing frees one, and re-claiming a tick that already holds its slot cannot deadlock a crash recovery."
}