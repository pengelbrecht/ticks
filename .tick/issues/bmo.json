{
  "id": "bmo",
  "title": "substrate = cloud: tk cloud spawn/wait/collect/reconcile for local orchestrators",
  "description": "D19: the cloud substrate must be drivable by ANY orchestrator location, not only the Workflow-hosted one. Expose the run API as the tk cloud verb family mirroring tk herd: spawn, wait, collect, reconcile. A local Claude Code, omp or Pi session sets substrate = \"cloud\" in runners.toml and drives worker containers from the laptop — local judgement, cloud hands.\n\nConsequences to implement, not just document:\n- Five parallel implementers stop costing local worktrees, CPU or battery.\n- A lid closed mid-wave loses only the orchestrator, which is the failure every runner already recovers from.\n- ONE LEASE wherever the orchestrator sits: on an enrolled project a LOCAL run takes the same RunRoom lease as a cloud run, or a laptop session and a scheduled run collide on .tick/. Un-enrolled projects keep the local file lease; enrolment upgrades the arbiter and never installs a second one.\n- The degenerate case stays sacred: a fully local orchestrator on the harness or herdr substrate with no cloud enrolment keeps working offline, forever.",
  "notes": "2026-08-22 06:04 - Seam (wave 6, three wide with s7f and t9s). You own the tk cloud spawn/wait/collect/reconcile verbs and their Go side. s7f owns reconcile-on-reboot inside the Run Workflow; t9s owns the TUI. ddv just made cloud a real Substrate value, so this tick is what makes declaring it mean something for a LOCAL orchestrator: mirror the tk herd verb vocabulary rather than inventing a parallel one, because the whole point is that an orchestrator swaps substrate without relearning its commands. Note the asymmetry this session measured — a local herdr wave lost two of three workers to a closed laptop lid while a cloud run finished an epic unattended; that is the reason this path is wanted.",
  "status": "in_progress",
  "priority": 2,
  "type": "feature",
  "owner": "noreply@anthropic.com",
  "blocked_by": [
    "0ds",
    "tap",
    "b6e",
    "ddv"
  ],
  "parent": "1vn",
  "acceptance_criteria": "A local orchestrator with substrate = cloud runs a wave in cloud containers and integrates it; the same run refuses when a cloud run holds the lease, naming the holder; a project with no cloud enrolment still runs fully offline on the herdr substrate.",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-08-20T02:34:44.005258Z",
  "updated_at": "2026-08-22T04:04:59.378948Z",
  "started_at": "2026-08-22T04:04:59.378947Z"
}