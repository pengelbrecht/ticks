{
  "id": "79x",
  "title": "Per-repo sandbox definition as a schema'd [sandbox] table in runners.toml",
  "description": "The operator wants each repo to declare its own sandbox. This completes the image story: the batteries-included base covers the common set, and a repo that needs more says so in tracked config rather than forcing a factory-wide image rebuild.\n\nAdd a Sandbox section to .tick/config.md, the established runner-neutral contract for dispatched implementers:\n- image: optional custom image reference. Absent means the version-pinned base image — the 99% path declares nothing.\n- toolchain: extra tools to provision through the version manager already shipped in the base (e.g. rust 1.x), resolved on first run into the project persistent sandbox and warm thereafter.\n- setup: idempotent, cache-populating commands run once per sandbox (pnpm install --frozen-lockfile in cloud/factory, go mod download). This closes a gap named twice during Phase 0: the existing Environment section is VERIFICATION only (test, do not ask) and cannot express provisioning.\n\nSECURITY BOUNDARY, and the reason this belongs in tracked config rather than anywhere else: setup runs arbitrary shell inside a sandbox holding credentials. It must come only from the tracked, PR-reviewed repo config on the submitted SHA — never from a tick note, a model, a signal payload, or an API parameter. Same rule the design doc applies to webhook sources: adding capability is a PR, not a dashboard click.\n\nLocal runs read the same section, so a herdr worker warms identically — this must not become cloud-only config.\n\nConsider (do not assume): a repo may already carry devcontainer.json declaring much of this. Reading it as an import path is worth evaluating, but a partial implementation of a large spec is worse than a small explicit section — if you support it, state exactly which keys are honoured and ignore the rest loudly.",
  "notes": "2026-08-19 18:38 - [human] FORMAT DECISION (human, 2026-08-19) — supersedes the original '.tick/config.md Sandbox section' framing in this tick's description. Markdown is the wrong container for structured, machine-parsed, fail-closed configuration.\n\nPut the sandbox definition in .tick/runners.toml as a [sandbox] table, and extend skills/ticks/references/runners-config.schema.json to cover it. Rationale:\n- runners.toml is already the 'how work executes' file ([orchestration] lives there) and is already the only tick config surface with a JSON schema.\n- The Sandbox setup key runs ARBITRARY SHELL in a credentialed sandbox. Parsing executable commands out of prose is precisely the hazard the Acceptance Evidence design already had to defend against — its verbatim-and-uniquely-matching, fails-closed-on-duplicate/ambiguous/injected rules exist ONLY because markdown has no structure. A schema gives uniqueness, typing and validation for free.\n- The working rule: if a PROGRAM parses it and a mistake fails closed, it needs a schema; if a MODEL reads it, markdown is right. Rules and the testing hints stay in config.md because implementers read them verbatim into a prompt; sandbox config does not.\n\nShape: [sandbox] with optional image (absent = version-pinned base), toolchain (extras via the mise in the base), and setup (idempotent, cache-populating commands). Validate against the schema and FAIL CLOSED on an unknown key rather than ignoring it.\n\nThe security boundary from the original description is unchanged and is now easier to enforce: setup is read only from the tracked runners.toml at the submitted SHA — never from a tick note, a model, a signal payload, or an API parameter.\n\nNOT in this tick, flagged for the human: config.md's 'Pi Orchestrator' section is structured key/value config in markdown that duplicates role and tier routing runners.toml already expresses with a schema, and the Acceptance Evidence / Closeout Evidence Commands mappings are structured data too. Migrating those is a separate decision touching shipped behaviour.",
  "status": "open",
  "priority": 2,
  "type": "feature",
  "owner": "noreply@anthropic.com",
  "blocked_by": [
    "3q2"
  ],
  "parent": "ko8",
  "acceptance_criteria": "A repo with no Sandbox section runs on the base image unchanged; a repo declaring extra toolchain gets it provisioned on first run and cached after; setup commands run once per sandbox and are idempotent on re-run; setup is read only from the tracked config at the submitted SHA, proven by a test that a tick note or API parameter cannot inject a command; the same section warms a local herdr worker.",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-08-19T16:32:48.090711Z",
  "updated_at": "2026-08-19T16:38:53.059584Z"
}