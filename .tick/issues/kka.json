{
  "id": "kka",
  "title": "Phase 2 — publish the cross-language contract",
  "description": "EIGHT CORE GO TESTS read fixtures out of cloud/factory/test/fixtures/ — the dependency runs backwards from what anyone would predict. They are deliberate Go/TS drift detectors: .tick/runners.toml is parsed by internal/herd/config AND re-implemented in cloud/factory/src/{toml,repo-config}.ts, and the fixtures are what stop the two diverging. Same for sandbox worker boot and message context. internal/gatewaytrace/trace_test.go:332-342 pins constants against cloud/factory/src/gateway.ts by convention. Move the fixtures into a versioned artifact both repos consume (skills/ticks/references/runners-config.schema.json is the natural seed) and repoint both test suites at it. A naive extraction relocates these and silently disables eight drift detectors — the likeliest way this project does real damage.",
  "notes": "2026-08-27 11:54 - FROM TICK psh — a THREE-way vocabulary contract now held together only by a comment. Phase 2 should cover it.\n\nThe collect verdict and status strings are shared by THREE implementations: internal/herd/collect (local), internal/cloud/collect (cloud, laptop side), and cloud/factory/src/worker-collect.ts (the Worker). Until psh landed, the first two were the SAME TYPE by import, so drift between them was impossible by construction. They are now copies.\n\nThe failure mode is silent and it is the worst kind: if NEEDS_CONTEXT or ready-to-merge is re-spelled in one place, a cloud run and a herd run disagree about what happened to the same tick and NOTHING FAILS. The report just quietly means something different.\n\nSymbols in the contract: Verdict, ReadyToMerge, NoCommits, MissingResult, BoundaryViolation, StatusBlocked, StatusNeedsContext, ParseStatus.\n\nAND MORE THAN THE CONSTANTS: the statusLine regexp is part of it too — including the DONE_WITH_CONCERNS-before-DONE alternation order (get that wrong and DONE_WITH_CONCERNS parses as DONE, which is a verdict inversion) and the markdown decoration trim set. A fixture that only pins the four constants would miss both.\n\nThe psh implementer considered a parity test in internal/herd/collect importing internal/cloud/collect — legal, since the forbidden direction is the other way — and deliberately left it out rather than inverting the dependency in tests. That option is still open and may be cheaper than a full contract artifact for the Go-to-Go half; the Go-to-TS half needs the artifact regardless.",
  "status": "open",
  "priority": 1,
  "type": "epic",
  "owner": "noreply@anthropic.com",
  "blocked_by": [
    "3j4"
  ],
  "parent": "a4n",
  "acceptance_criteria": "Deliberately breaking cloud/factory/src/toml.ts still fails a Go test, with no test reading a fixture by relative path across the repo boundary. All eight detectors demonstrably still fire — show one failing on purpose. go test ./... and the factory vitest suite green.",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-08-27T09:03:04.281814Z",
  "updated_at": "2026-08-27T09:54:04.252503Z"
}