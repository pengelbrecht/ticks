{
  "id": "4ik",
  "title": "ticfac Phase 1: the reconciler and the local subprocess executor",
  "description": "SPEC §12 Phase 1. Create the separate ticfac repository with its own CI and ticfac run-epic \u003cid\u003e. Implement the tk client, Git integration command, evidence command and the restartable reconciliation loop for one epic at concurrency one — the deterministic lifecycle (claims, waves, gates, merge, boundary checks, cleanup ordering) in code. Implement the local subprocess executor through the four operations: worktree per attempt, runner launched headless, RESULT as completion contract; claude, codex and pi are runner values on this one executor. Profiles resolve to executor, runner, model, prompt only. Deliverables: ticfac repo, reconciler, subprocess executor, three profiles, restart demonstration.",
  "notes": "2026-09-02 13:29 - INHERITED FROM 5hm (properties of the move, not of the old architecture): (1) embedded.go at the module root welds skills + cloud/factory + cloud/sandbox into ONE go:embed unit because go:embed cannot reach above its package; tk factory deploy ships that bundle baked into the binary — that is today's version pin and the hardest structural blocker. (2) cloud/factory/required-tk-commands is DERIVED by a go/ast scan of cloudTkJSON call sites in cmd/tk/cmd; if call sites move the scanner moves with them or is replaced, or the derivation test passes while scanning nothing. (3) cloud/worker is the ticks.sh BOARD, not the factory; it stays. (4) the sandbox image installs two binaries, tk and ticfac, no shim; 'tk cloud ...' becomes 'ticfac ...' and every caller incl. container entrypoints is repointed as part of the move. Also: CHANGELOG 0.32.0 is held UNRELEASED until the extraction settles what ships from where. Prior decoupling work already done under a4n: Phase 1 (3j4) cut the Go dependency, Phase 3 (9hu) split the credential file, Phase 5a (g0b) cut the last cross-repo edges — build on those, do not redo them.\n2026-09-02 13:34 - LEGACY DEFECTS THAT MUST BE SOLVED BY DESIGN HERE (from epic 72y, closed as legacy 2026-09-02). Each is a required test in the Phase 1 gate or the Appendix A suite, not a fix to port:\n- iu9 worker wiped on sandbox sleep -\u003e Appendix A #5: the executor pushes in-progress work on a timer; RESULT presence is checked on origin, not in a container.\n- k4r runs stuck in 'stopping' forever -\u003e Appendix A #8: in-flight state is settled by whoever finds it next from durable evidence.\n- 4qx phantom boundary violation after a worker merged the integration branch -\u003e collect diffs the attempt branch against the attempt's recorded base SHA (in attempts/\u003cn\u003e.json), never against the moving target ref. Test: worker merges integration, boundary check stays clean.\n- 09w boundary check conflates tracker state with project config -\u003e the .tick/ boundary is enforced on tracker records (.tick/issues, activity) and explicitly exempts .tick/config.md, runners.toml and learnings.md, which workers may edit. Test both directions.\n- 5jo agent/handle names collide across repos (tick-\u003cid\u003e global namespace) -\u003e JobHandle identity is (repository coordination key, run id, tick, attempt); the Herdr executor names agents from that, never from tick id alone. Test: two repos, same tick id, concurrent.\n- gm5 report lands outside the worktree when the worker cds -\u003e the executor owns the RESULT path (absolute, inside the attempt worktree, passed in JobSpec.artifact_prefix); the worker's cwd is irrelevant. Test: worker cds to /tmp then writes RESULT.\n- 5qj flaky cloud/factory vitest under load -\u003e Phase 0 T4 (mrq) and Phase 4: compatibility tests must not depend on workerd timing; flake is a defect in the test, never a rerun.\n- 201 herd dashboard truncates the epic list -\u003e Phase 2 dashboard scrolls; cosmetic, no gate.\n2026-09-02 16:52 - Also applies here (Peter, 2026-09-02): the local subprocess executor must pass the same finished-worker-detection tests as the Herdr executor (see note on jrj (a)): completion from durable evidence only, no lost-event race, re-issued waits see settled work immediately, and worktree cleanup leaves zero run-created worktrees/branches.\n2026-09-02 17:14 - PHASE 1 PARTITION (planned at 692 close-out, 2026-09-02; create these in the ticfac tracker after cgb, foundation-first, each a vertical slice with the contract readers as tests):\nW1  bootstrap: Go module, CLI skeleton (ticfac run-epic \u003cid\u003e fails closed: no executor configured), CI that pins ticks at a ref and verifies contract bundle 3.0.0 (contracts.pin mode=pinned), and a ticfac Go reader for EVERY contract (job-protocol, ticfac-run-state, lifecycle-invariants, tk-json-manifest, credential-ownership, tracker-layout, collect-vocabulary, worker-boot, message-context, runners-config, sandbox-image, signal-source, sweep-*) so the consumer side of the two-reader rule is real. Seam owner for go.mod/go.sum.\nW2  (parallel, disjoint packages) tk client — every manifest command, --json-contract 1, fail closed on mismatch, fixture repo tests | .ticfac/ store — checkpoint/attempt/decision/evidence writer, CAS against origin exactly as the 7 sequences in ticfac-run-state.json, gitignore fragment, tag at terminal state | local subprocess executor — start/inspect/cancel/collect, worktree per attempt, runner claude|codex|pi headless, executor-owned RESULT path (gm5), timer push (A#5), identity (repo key, run, tick, attempt) (5jo), the finished-worker-detection and zero-orphan tests from the 2026-09-02 note.\nW3  reconciler for one epic at concurrency one: claims, waves, gates, merge, boundary on tracker records only with config exempt (09w) and diff against the recorded attempt base (4qx), cleanup ordering, restart from a clone alone; Appendix A suite run against the REAL reconciler+executor (not the fake).\nW4  profiles implement-tick / review-epic / closeout-epic resolving to executor, runner, model, prompt only; EPIC-SKELETON behaviour; role jobs dispatched through the same executor.\nW5  gate: one real ticks epic through ticfac run-epic with kill+restart at the three points; evidence record + tag; a report that closes 421 here.\nThen EPIC-SKELETON (review, closeout) in the ticfac tracker. Do not build an event bus, plugin system, or same-repo multi-epic to make W5 pass (SPEC §12).",
  "status": "open",
  "priority": 2,
  "type": "epic",
  "owner": "peter@engelbrecht.dk",
  "blocked_by": [
    "692"
  ],
  "parent": "hzm",
  "acceptance_criteria": "Gate: one real epic on the ticks repository completes through ticfac run-epic locally with a claude or codex runner, killed and restarted after dispatch, after collection and before closure, with no duplicate work and no false success.",
  "created_by": "noreply@anthropic.com",
  "created_at": "2026-09-02T11:28:24.454946Z",
  "updated_at": "2026-09-02T15:14:08.58524Z"
}